Written for Debian and uses only Debian-packaged Python libraries.

Requirements:
- Python packages
  - schedule (python3-schedule)
  - dbus-python (python3-dbus)
  - pyroute2 (python3-pyroute2)
  - selenium (python3-selenium)
- Chrome WebDriver (chromium-driver)
